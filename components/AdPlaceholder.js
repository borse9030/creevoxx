import React from "react";

export default function AdPlaceholder({ size = "leaderboard", id }) {
  // Returning null completely removes the empty AdSense blocks from the screen
  // until you create a Monetag Banner zone to replace it.
  return null;
}
